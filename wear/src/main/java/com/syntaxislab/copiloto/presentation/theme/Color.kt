package com.syntaxislab.copiloto.presentation.theme

import androidx.compose.ui.graphics.Color

// ==========================================
// Paleta de Colores Oficial - Co-piloto Wear
// ==========================================

// Negro Base (Fondo de pantalla): #000000 (Puro / OLED)
val NegroBase = Color(0xFF000000)

// Negro Superficie (Tarjetas y contenedores): #121212
val NegroSuperficie = Color(0xFF121212)

// Amarillo Alta Visibilidad (Badges y destacados): #FFEA00
val AmarilloAltaVisibilidad = Color(0xFFFFEA00)

// Naranja Fuego (Botones principales / FAB): #FF6D00
val NaranjaFuego = Color(0xFFFF6D00)

// Blanco Texto (Títulos y principal): #FFFFFF
val BlancoTexto = Color(0xFFFFFFFF)

// Gris Texto Secundario (Subtítulos): #A1A1AA
val GrisTextoSecundario = Color(0xFFA1A1AA)

// --- Alias en inglés para conveniencia ---
val BlackBase = NegroBase
val SurfaceBlack = NegroSuperficie
val HighVisYellow = AmarilloAltaVisibilidad
val FireOrange = NaranjaFuego
val TextWhite = BlancoTexto
val TextSecondaryGray = GrisTextoSecundario
